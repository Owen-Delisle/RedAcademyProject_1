const styles = theme => ({
  flexContainer: {
    backgroundColor: 'black',
    padding: 100,
    alignItems: 'center',
    justifyContent: 'center'
  },
  cards: {
    paddingTop: 300
  }
});

export default styles;
